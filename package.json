{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "npm run towatch && nuxt dev",
    "serve": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "towatch": "node frame/service/service.js --type=watch",
    "create": "node frame/service/service.js --type=create"
  },
  "devDependencies": {
    "ejs": "^3.1.8",
    "nuxt": "3.0.0-rc.8",
    "sass": "^1.54.4"
  },
  "dependencies": {
  }
}
